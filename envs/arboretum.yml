# a minimal environment for larch v6
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.10
  - pip
  - addicty
  - dask
  - filelock
  - networkx
  - numba >= 0.51.2
  - numexpr
  - numpy >= 1.19
  - pandas >= 2.0
  - pyarrow
  - rich
  - scipy
  - sharrow >= 2.7
  - sparse
  - xarray
  - xmle
  - pip:
      - larch6
